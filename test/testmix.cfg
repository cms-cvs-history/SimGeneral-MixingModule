
process PRODTMIX  = {

	source = PoolSource { 
		untracked vstring fileNames = {"file:/tmp/uberthon/Cum.root"}
		untracked int32 maxEvents = 5
	}

	module test = TestMix {
		untracked int32 PrintLevel=2
	}
	path p = { test}
}
