
process PROD  = {

	source = PoolSource { 
		untracked vstring fileNames = {"file:Cum.root"}
		untracked int32 maxEvents = 5
	}

	module test = TestMix {
		untracked int32 PrintLevel=2
	}
	path p = { test}
}
