
process PROD  = {

	source = PoolSource { 
		string fileName = "SimGeneral/MixingModule/data/Cum.root"
		untracked int32 maxEvents = 1
	}

	module test = TestMix {
		untracked int32 PrintLevel=2
	}
	path p = { test}
}
