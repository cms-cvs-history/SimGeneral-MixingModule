
process PROD  = {

	source = PoolSource { 
		vstring fileNames = {'data/simevent.root'}

		untracked int32 maxEvents = 1
	}

	module out = PoolOutputModule {	
	       string fileName = 'Cum_xxx.root'
	}

	module mix = MixingModule {
	       secsource input = PoolRASource  {
			       vstring fileNames = {'data/simevent.root'}
			       string type = "fixed"
		               double averageNumber = 1
		               int32 minBunch = xxx
		               int32 maxBunch = xxx
			       int32 seed = 1234567
                               untracked int32 maxEvents = 1
                               untracked uint32 maxEventsToSkip = 0
	       }
	       int32 bunchspace = 25
	}
	path p = { mix, out }
}
