
process PROD  = {

	source = PoolSource { 
		string fileName = "Cum_xxx.root"
		untracked int32 maxEvents = 1
	}

	module test = TestSuite {
		untracked int32 BunchNr = xxx
		untracked int32 minBunch = bcrs
		untracked int32 maxBunch = bcre
		string fileName = "histos.root"
	}
	path p = { test}
}
